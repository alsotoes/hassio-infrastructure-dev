#include <tunables/global>

profile garage flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>

  network inet stream,
  network inet6 stream,
  capability dac_override,
  capability sys_resource,
  capability setgid,
  capability setuid,

  /usr/local/bin/garage mr,
  /etc/garage.toml r,
  /data/** rw,
  /run/** rw,
  /tmp/** rw,
}