#!/command/with-contenv bash
# shellcheck shell=bash

# Graceful shutdown - Garage handles SIGTERM properly
# This script runs after the main process exits

bashio::log.info "Garage service stopped"
exit 0